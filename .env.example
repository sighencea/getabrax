# Supabase Configuration
# Get these from your Supabase project settings: https://app.supabase.com/project/_/settings/api

VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Note: The following secrets should be set in Supabase Edge Functions settings,
# NOT in this .env file:
# - ALTCHA_HMAC_KEY (generate with: openssl rand -hex 32)
# - SUPABASE_SERVICE_ROLE_KEY (already available in Edge Functions by default)
